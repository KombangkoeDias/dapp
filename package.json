{
  "scripts": {
    "start": "concurrently --kill-others \"cd client && yarn start\" \"cd API && yarn start\"",
    "install-global": "yarn",
    "install-client": "cd client && yarn",
    "install-api": "cd API && yarn",
    "install-api-contract": "cd API/Functions && yarn",
    "install-dependencies": "yarn install-global && yarn install-client && yarn install-api && yarn install-api-contract"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.1.0",
    "concurrently": "^6.2.0"
  }
}
